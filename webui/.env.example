# Optional: base URL of the static file server when webui and server are deployed separately.
# Leave unset for same-origin (dev proxy or same host).
# Example: webui on this machine, static server on another machine on the LAN:
# VITE_STATIC_SERVER_BASE=http://192.168.1.100:3456
