# Workbook Index

| 日期 | 主题 | 关键文件 |
|------|------|---------|
| 2026-04-24 (2) | PersonaPostureLayer integration：统一 PersonaPostureBias 类型（去重，canonical 在 layer），SMOOTHING_TAU_MS 400→1200ms（1-2s 目标），层栈位移（PersonaPosture 紧跟 WalkingLayer），biome 格式修复；497 tests pass | packages/avatar/src/{AvatarService,index,compiler/AnimationCompiler,compiler/layers/{PersonaPostureLayer,EyeGazeLayer,index}}.ts + tests |
| 2026-04-24 | Avatar SoVITS 端到端流式 + PCM 分片：`TTSProvider.synthesizeStream?()` + SovitsProvider `streaming_mode` raw-PCM；`AudioChunkMessage` 契约源 + `PreviewServer.broadcastAudioChunk`；`decodeToMonoPcm` 加 `audio/pcm` 分支 + `RmsStreamer` 跨 chunk RMS；`AudioEnvelopeLayer` streaming mode（`appendFrames`/`finalize`，streaming guard throws）；`SpeechService.drain()` 拆 `drainOneBuffered`/`drainOneStreaming`，零延迟契约；全 ticket 110 测试全绿（3 个 AmbientAudioLayer 失败为 master 既有） | packages/avatar/src/{tts/**,preview/**,compiler/audio/{decodeToMonoPcm,rmsStreaming}*,compiler/layers/AudioEnvelopeLayer*,SpeechService*,AvatarService}.ts, packages/bot/src/core/bootstrap.ts, config.example.jsonc |
| 2026-04-23 (2) | PerlinNoiseLayer activity envelope：解决"头一直转圈从不停"——单 perlin 输出恒非零 → two-perlin（motion × envelope）架构，慢 perlin（0.06–0.08 Hz）经两阈值 smoothstep（`envPauseBelow=-0.1`/`envActiveAbove=0.45`）映射到 `[0,1]` 乘 motion。向后兼容：`PerlinChannelConfig` 加 4 字段可省略；`envActiveAbove:-10` escape hatch 回退老行为；per-channel 独立 `envelopeSeed` 让 yaw/pitch/roll 有机错开。实测 ~55% 帧近零、活动占空比<65%、pause/active run 中位数 ~数秒。+4 测试（静止窗口/占空比/window peak 方差/escape hatch） | packages/avatar/src/compiler/layers/{PerlinNoiseLayer,PerlinNoiseLayer.test}.ts |
| 2026-04-20 | Avatar AmbientAudioLayer — 30Hz ambient-audio WS → body.z/brow BGM reactivity layer | packages/avatar/src/compiler/layers/{AmbientAudioLayer.ts,AmbientAudioLayer.test.ts}, packages/avatar/src/preview/{types,PreviewServer}.ts, packages/avatar/src/AvatarService.ts |
| 2026-04-20 (2) | Avatar Spring-Damper: replace single-pole low-pass with per-channel (omega, zeta) spring-damper in AnimationCompiler.tick() | packages/avatar/src/compiler/{AnimationCompiler.ts,types.ts,AnimationCompiler.test.ts} |
| 2026-04-20 (3) | Avatar Tunable Params API: WS contract (TunableParam/Section + 3 msg types) + AnimationLayer get/setTunableParam + AmbientAudioLayer 5 params + AudioEnvelopeLayer shared singleton + AnimationCompiler springOverrides + PreviewServer tunable-params handlers + AvatarService wire | packages/avatar/src/preview/{types,PreviewServer}.ts, packages/avatar/src/compiler/layers/{types,AmbientAudioLayer,AudioEnvelopeLayer,audio-envelope-config}.ts, packages/avatar/src/compiler/{AnimationCompiler,AnimationCompiler.tunable.test.ts}, packages/avatar/src/compiler/layers/AmbientAudioLayer.tunable.test.ts, packages/avatar/src/AvatarService.ts |
| 2026-04-14 | Video 分析工具链修复：新增 VideoDownloadService/ResourceCleanupService、修正 executor 双重上传、preset timeout/maxToolRounds 配置、测试重写 | src/services/video/, src/tools/executors/AnalyzeVideoToolExecutor.ts, GeminiProvider.ts |
| 2026-04-14 (2) | SubAgent 返回类型链 `unknown` → `string` 全链路修复 + Video 命令 URL 提取增强（裸域名、裸 BV 号、YouTube shorts、混合文本、通用 URL fallback） | src/agent/, src/ai/AIService.ts, VideoAnalyzeCommandHandler.ts, VideoAnalyzePlugin.ts |
| 2026-04-14 (3) | light_app 视频 URL 提取：segmentsToText 附带 jump link URL + VideoAnalyzeCommandHandler 显式解析 light_app segment（仅提取视频 URL） | MilkyMessageSegmentParser.ts, VideoAnalyzeCommandHandler.ts |
| 2026-04-14 (4) | Cluster：markTaskCompleted 加 parentTaskId 守卫修复 hub_spawn 子任务误触发 job-complete + cascade-kill；/workers 端点 union 内存 registry 与从 cluster_tasks 重建的历史 worker，跨重启可见 | ClusterScheduler.ts, ClusterAPIBackend.ts |
| 2026-04-15 | 审计 Modal 与 Recent Jobs 统一用 JobRow；workers 从内联列表改为 `Workers (N)` 按钮 → 新增 JobWorkersModal（按 role 分组 + reportStatus 图标，点 worker 打开 WorkerDetailModal） | webui/src/pages/cluster/components/JobRow.tsx, JobWorkersModal.tsx, HistoryModal.tsx, index.tsx |
| 2026-04-16 | WebUI ticket editor paste-lift frontmatter + Select 组件化（TemplateSelect role 分组、StatusSelect 色点、RegistryProjectSelect Radix 改写）+ Radix Portal 暗色模式字色修复；ClusterPage template select 复用 | webui/src/pages/tickets/frontmatter.ts, webui/src/components/{TemplateSelect,StatusSelect,RegistryProjectSelect}.tsx, TicketEditor.tsx, cluster/index.tsx |
| 2026-04-18 | Avatar Preview Server：本地 + LAN WebSocket 实时预览 Live2D 参数与动画状态，Bun.serve 同一端口提供 HTTP 页面和 WS 广播，0.0.0.0:8002 绑定支持多机访问 | src/avatar/preview/ |
| 2026-04-18 (2) | Avatar Idle State Machine：5 态 BotState 状态机 + idle 随机待机动画（setTimeout 链 3-8s）+ 过渡动画输出（thinking duration=0 持续），StateNodeOutput 类型独立于 compiler | src/avatar/state/ |
| 2026-04-18 (3) | Avatar Driver Adapter：抽象 `DriverAdapter` (EventEmitter) + VTSDriver（VTS WebSocket API 认证握手 + token 持久化 + InjectParameterDataRequest + 指数退避重连 + 30fps 节流）；Avatar Animation Compiler 核心模块 | src/avatar/drivers/ |
| 2026-04-18 (4) | T1: webui 迁移到 packages/webui，启用 bun workspaces，更新 dev/web/build:admin/pm2-ui.sh/start.sh | package.json, packages/webui/, scripts/pm2/pm2-ui.sh, start.sh |
| 2026-04-18 (5) | T2: src/ 迁移到 packages/bot/，TypeScript project references，PluginManager hardcoded path 修复，smoke-test/test cwd 约束 | packages/bot/, tsconfig.base.json, PluginManager.ts |
| 2026-04-18 (6) | Avatar LLM 情感标签系统（prompt + 解析器） | prompts/avatar/, packages/bot/src/avatar/tags/ |
| 2026-04-18 (7) | Avatar Foundation REDO: AvatarService 重写（完整子系统连线 compiler/stateMachine/driver/previewServer）+ config wiring（BotConfig avatar 字段 + getAvatarConfig() + bootstrap + index.ts + config.example.jsonc） | packages/bot/src/avatar/AvatarService.ts, core/config/index.ts, core/bootstrap.ts, src/index.ts, config.example.jsonc |
| 2026-04-18 (8) | Live2D Avatar Plugin（6 hooks 把 bot 生命周期映射到 AvatarService.transition + tag strip）+ PromptAssemblyStage 注入 avatar emotion-system prompt（运行时 DI 检查） | packages/bot/src/plugins/plugins/Live2DAvatarPlugin.ts, ai/pipeline/stages/PromptAssemblyStage.ts, config.example.jsonc |
| 2026-04-18 (9) | T3: avatar 抽成独立包 @qqbot/avatar | packages/avatar/, tsconfig.json, packages/bot/tsconfig.json, packages/bot/package.json, bootstrap.ts, Live2DAvatarPlugin.ts, PromptAssemblyStage.ts |
| 2026-04-18 (10) | ClusterTicketWriteback 加 `job.json` 结构化快照 + clusterId，跨 LAN webui 打开 ticket 能看到 job/worker 结果；re-dispatch 覆盖 + stale task 文件清理 | packages/bot/src/cluster/{config.ts,ClusterTicketWriteback.ts}, packages/webui/src/api.ts, packages/webui/src/pages/tickets/components/TicketDetailPanel.tsx, config.d/cluster.jsonc, config.example.jsonc |
| 2026-04-18 (11) | Avatar Config 收口：默认 action-map 内置到包（`packages/avatar/assets/default-action-map.json`），`.vts-token` 改走 `data/avatar/`，清理仓库根 `config/avatar/` | packages/avatar/src/compiler/action-map.ts, packages/avatar/src/types.ts, packages/avatar/src/drivers/types.ts, packages/avatar/src/AvatarService.ts, packages/bot/src/core/bootstrap.ts, config.example.jsonc |
| 2026-04-19 | Avatar AmbientDriver 原语（连续正弦驱动）+ baseline-breath 默认 driver（4 channels，对标 Cubism SDK 4 native breath）+ compiler/AvatarService wire-up + opt-in config；+ PreviewServer 双向 WS（客户端 trigger 消息 → enqueueTagAnimation） | packages/avatar/src/compiler/{AmbientDriver,default-drivers,AnimationCompiler,types}.ts, packages/avatar/src/{types,AvatarService}.ts, packages/bot/src/core/bootstrap.ts |
| 2026-04-18 (12) | Avatar runtime integration 故障排查：第一次 enabled=true 跑 VTS on LAN 暴露 7 个集成 bug + VTS `InjectParameterData` 只收 tracking param 的架构限制；引入语义 `channel` 层（`head.yaw`/`mouth.smile`）+ VTSDriver 翻译 `VTS_CHANNEL_MAP`；compiler tick 数学重写（动画结束 param 从 emit drop，VTS 自然接管）；PreviewServer WS upgrade 路由修复 + 客户端双 ws 连接修复；Plugin 6 hook 加 `isPrivate()` gate + tag 真入队 compiler | packages/avatar/src/{compiler/\*,drivers/\*,preview/\*,state/idle-animations.ts,AvatarService.ts}, packages/avatar/assets/default-action-map.json, packages/bot/src/plugins/plugins/Live2DAvatarPlugin.ts |
| 2026-04-19 (2) | Avatar AnimationLayer 架构重构：`compiler/layers/` 四层（BreathLayer 多谐波 + AutoBlinkLayer 4 阶段 always-emit + EyeGazeLayer OU+saccade dt≤100ms + IdleMotionLayer 简化 keyframe clip）+ `LayerManager` 替代 AmbientDriver + DEFAULT_LAYER_GATE；`channels/` 注册中心（cubismParam 必填，VTS 降级可选 alias，删 `drivers/vts-channel-map.ts`）；compiler pause/resume 消费端门控（VTS connected + preview client count 0↔1 切换）；`mergeAvatarConfig` 合入 `AvatarService.initialize`，bootstrap 块 28→14 行；action-map `eye.open` 改负向 delta；commit `bf0de23` | packages/avatar/src/{compiler/layers/*,channels/*,config.ts,AvatarService.ts,state/{IdleStateMachine,transition-animations}.ts,preview/PreviewServer.ts,drivers/VTSDriver.ts,compiler/AnimationCompiler.ts}, packages/avatar/assets/default-action-map.json, packages/bot/src/core/bootstrap.ts, qqbot-avatar-renderer/src/channel/cubism-channel-map.ts |
| 2026-04-19 (3) | Avatar polish：`ParamTarget.oscillate` 周期性动作（shake_head/nod/wave 正确建模）+ blink action 回归 + arm.left/right + eye.smile.left/right 通道；renderer silent-drop 修复（breath / eye.ball.x/y）+ cross-model `buildChannelMap(overrides)` + `alsoWrite` fan-out 替代 brow 硬编码 + per-model `/assets/models/<slug>/channel-map.json` override；action-map HTTP 共享（`/action-map` JSON + `category` 字段 + HUD `useActionList` 动态 groups）；commit `c28841a` | packages/avatar/src/{compiler/{types,AnimationCompiler,action-map}.ts,channels/registry.ts,preview/PreviewServer.ts,AvatarService.ts}, packages/avatar/assets/default-action-map.json, qqbot-avatar-renderer/src/{channel/cubism-channel-map.ts,channel/apply.ts,app/CubismRenderer.ts,types/protocol.ts,app/ChannelClient.ts,ui/Hud.tsx} |
| 2026-04-19 (4) | TTS 接入前期铺路：方案讨论（bot 编排 + 引擎各处 + queue 串行 + RMS 非 viseme）+ renderer A2 直接落地（`AudioMessage` 协议 + `ChannelClient.onAudio` + `SpeechPlayer` 单 `<audio>` 调度播放，`combined`/`model` view 播音频 `hud` 静音）+ 3 张 cluster-tickets（A1 avatar-speech-service 契约源 / B tts-provider-registry Fish+SoVITS bodyTemplate 占位符 / C avatar-lipsync-envelope RMS+ffmpeg decoder+AudioEnvelopeLayer per-utterance ephemeral） | qqbot-avatar-renderer/src/{types/protocol.ts,app/ChannelClient.ts,app/SpeechPlayer.ts,main.ts}, cluster-tickets/qqbot/2026-04-19-{avatar-speech-service,tts-provider-registry,avatar-lipsync-envelope}/ticket.md |
| 2026-04-19 (5) | Avatar SpeechService 业务层接线 (Task 3)：PreviewServer.broadcastAudio + AvatarConfig.speech 字段 + AvatarService.hasConsumer/speak/initialize(ttsConfig) + bootstrap 传 getTTSConfig + Live2DAvatarPlugin 调 speak + config.example.jsonc speech 注释 | packages/avatar/src/{preview/PreviewServer.ts,types.ts,config.ts,AvatarService.ts}, packages/bot/src/core/bootstrap.ts, packages/bot/src/plugins/plugins/Live2DAvatarPlugin.ts, config.example.jsonc |
| 2026-04-19 (6) | TTS Provider Registry 收尾（B ticket 被 kill 后的四项缺口补完）：TTSProvider.listVoices? 可选 + FishAudio 实现；provider 构造加 optional name；avatar 包导出 TTSManager/TTSProvider/FishAudio/SovitsProvider；DITokens.TTS_MANAGER；bootstrap 构建 TTSManager（`tts.providers[]` + legacy `{apiKey}` shim + setDefault fallback）+ 注册进 DI；AvatarService.initialize 改接 TTSManager；TTSCommandHandler 重写（`@inject(TTS_MANAGER)` + `--provider` flag + 按 provider 展示 voices）；config.example.jsonc tts 块改成 `{defaultProvider, providers:[...]}` + SoVITS 注释样例 | packages/avatar/src/{AvatarService.ts,index.ts,tts/{TTSProvider.ts,providers/{FishAudio,Sovits}Provider.ts}}, packages/bot/src/{core/{DITokens.ts,bootstrap.ts,config/types/tts.ts},command/handlers/TTSCommandHandler.ts}, config.example.jsonc |
| 2026-04-19 (7) | HUD speak 调试面板 + TTS 诊断日志：SpeakMessage 跨 2 repo 契约（`{type:'speak',data:{text}}`）→ PreviewServerHandlers.onSpeak → AvatarService.speak 直接绕过 LLM；Hud `<SpeakDebugPanel>` TextArea + Cmd/Ctrl+Enter submit；Live2DAvatarPlugin/SpeechService/handlePreviewClientCount 全链路 log（speak gate / consumer / synthesize start-ok / broadcast / ws count changed ticking=?） | packages/avatar/src/{preview/{types,PreviewServer}.ts,AvatarService.ts,SpeechService.ts}, packages/bot/src/plugins/plugins/Live2DAvatarPlugin.ts, qqbot-avatar-renderer/src/{types/protocol.ts,app/ChannelClient.ts,ui/Hud.tsx} |
| 2026-04-19 (8) | `/avatar <text>` 命令取代自动 speak：Live2DAvatarPlugin.onMessageBeforeSend 删 speak 调用（避免 CardRenderingService 把长回复渲染成图）；新 AvatarCommandHandler 走独立 LLM + 专用 prompt（第一人称 / 1-3 句短口语 / 带 Live2D tag）+ 解析标签驱动动画 + `avatar.speak` TTS + 返回纯文本 segments（命令 bypass reply pipeline 的 card render）；新 prompt 模板 `prompts/avatar/speak-system.txt` PromptManager 按目录派生 key `avatar.speak-system` | packages/bot/src/plugins/plugins/Live2DAvatarPlugin.ts, packages/bot/src/command/handlers/{AvatarCommandHandler.ts,index.ts}, prompts/avatar/speak-system.txt |
| 2026-04-19 (9) | `/avatar` state 卡 reacting + 字幕 toast UI：AvatarCommandHandler 删 tagToBotState+transition（state 交给 pipeline hook），AudioMessage.text 字段跨 2 repo，Hud useActiveUtterance + SpeakToast 底部居中固定覆盖（隐藏 bubble 模式仍渲染） | packages/bot/src/command/handlers/AvatarCommandHandler.ts, packages/avatar/src/{preview/types,SpeechService}.ts, qqbot-avatar-renderer/src/{types/protocol,ui/Hud}.tsx |
| 2026-04-19 (10) | Avatar state 建模重构 BotState → AvatarActivity(ambientGain+pose)：拆三件正交事（全局 gain / idle gate / pose 动画），LayerGate 删除 LayerManager 直读 ambientGain，IdleMotionLayer 从 state==='idle' 改派生式 (pose==='neutral' && gain>=1)，setActivity(partial) API 取代 transition(state)，PreviewStatus 字段 pose+ambientGain 拆开，Plugin hook 用 AMBIENT 常量表达两轴 | packages/avatar/src/{state/\*,compiler/{AnimationCompiler,layers/\*},AvatarService,preview/types}.ts, packages/bot/src/{plugins/plugins/Live2DAvatarPlugin,command/handlers/AvatarCommandHandler}.ts, qqbot-avatar-renderer/src/{types/protocol,ui/Hud}.tsx（commit `9ea9e72`） |
| 2026-04-19 (11) | 动态注入 action-map 到 LLM prompt：检出 prompt 模板写死 8 动作 vs action-map 实际 16 动作的漂移，ActionMapEntry+ActionSummary 加可选 description 字段，16 条 entry 填中文一行描述，新 formatActionsForPrompt helper（按 emotion→movement→micro 分组），两 prompt 模板 {{availableActions}} 占位符，PromptAssemblyStage 和 AvatarCommandHandler 渲染时从 avatar.listActions() 拼入 | packages/avatar/{assets/default-action-map.json,src/{AvatarService,compiler/{types,action-map}}.ts}, prompts/avatar/{emotion-system,speak-system}.txt, packages/bot/src/{ai/pipeline/stages/PromptAssemblyStage,command/handlers/AvatarCommandHandler}.ts（commit `5313144`） |
| 2026-04-19 (7) | Avatar LipSync RMS Envelope (Task 1+2)：computeRmsEnvelope + decodeToMonoPcm（ffmpeg 主路径 + audio-decode@3.9.3 fallback）+ AudioEnvelopeLayer per-utterance ephemeral；SpeechService 构造加 registerLayer/unregisterLayer callbacks + drain() 解码 + envelope 注册 + setTimeout unregister；AvatarService 传 compiler.registerLayer/unregisterLayer | packages/avatar/src/{compiler/audio/{rms,decodeToMonoPcm}.ts,compiler/layers/{AudioEnvelopeLayer.ts,index.ts},SpeechService.ts,AvatarService.ts}, packages/avatar/package.json, bun.lockb |
| 2026-04-19 (13) | Avatar PerlinNoiseLayer 微动层（Task 2）：手写 1D Perlin（fade 6t⁵-15t⁴+10t³、per-channel 512-entry permutation、mulberry32 确定性种子）+ 5 通道（head.yaw/pitch/roll body.x/y）+ 默认栈最末 weight=0.2 + stateless sample(nowMs) 加法混合 | packages/avatar/src/compiler/layers/{PerlinNoiseLayer.ts,PerlinNoiseLayer.test.ts,default-layers.ts,index.ts} |
| 2026-04-19 (14) | Live2DPipeline 5 stage 化（Gate/PromptAssembly/LLM/TagAnimation/Speak 独立 @injectable）+ Bilibili 弹幕接收链路全新增（BilibiliLiveClient 手写 WS 协议/帧解码/brotli+zlib 嵌套/心跳/重连 + DanmakuBuffer 3s 聚合去重 + DanmakuStore 异步落库 SQLite+MongoDB + bilibili_danmaku 表 + BilibiliLiveBridge 桥接 + bilibili-batch-system prompt + BilibiliConfig）；AvatarCommandHandler 重构为 enqueue 薄壳；commit `7d80194` | packages/bot/src/services/{live2d/\*,bilibili/live/\*}, packages/bot/src/database/{models/types,adapters/{SQLite,MongoDB}Adapter}.ts, packages/bot/src/{core/{DITokens,bootstrap,config/{index,types/bilibili}}.ts,index.ts,command/handlers/AvatarCommandHandler.ts}, prompts/avatar/bilibili-batch-system.txt |
| 2026-04-19 (15) | `/live2d <connect\|disconnect\|reconnect\|status>` 命令 + `bilibili.live.autoConnect` 默认 false + auth 修复三连：WBI 签名 getDanmuInfo（裸请求 -352）、buvid3 从 spi endpoint、**auth packet `platform='danmuji'` 而非 `'web'` 绕过浏览器指纹检测（参考 bililive_dm C# 客户端 DanmakuLoader.cs:360）**；SESSDATA+danmuji 冲突 → WS 接收路径强制匿名，SESSDATA 只给 sendDanmaku REST 用；reconnectAttempts 归零时机改到 AUTH_REPLY code=0；3 次 pre-auth 失败硬停；-352/-412 风控码 60s 回退地板；SESSDATA 自动归一化（`,`→`%2C`、`*`→`%2A` idempotent）；中间装 bilibili-live-ws 验证后卸掉（库默认也是 platform=web 且 getConf 裸请求）；commit `fb3bf83` | packages/bot/src/services/bilibili/live/{BilibiliLiveClient,BilibiliLiveBridge}.ts, packages/bot/src/command/handlers/{Live2DCommandHandler,index}.ts, packages/bot/src/core/{DITokens,bootstrap,config/types/bilibili}.ts, packages/bot/src/index.ts, config.example.jsonc |
| 2026-04-19 (16) | Avatar AudioEnvelopeLayer extended to drive body.z / eye.open.* / brow from audio energy | packages/avatar/src/compiler/layers/{AudioEnvelopeLayer.ts,AudioEnvelopeLayer.test.ts} |
| 2026-04-21 | endPose/crossfade/channelBaseline compiler 三件套 + 3 条 demo action + tunable section compiler:envelope + PreviewStatus 扩字段 | packages/avatar/src/compiler/{types,action-map,AnimationCompiler,AnimationCompiler.test,AnimationCompiler.tunable.test}.ts, packages/avatar/assets/default-action-map.json, packages/avatar/src/{preview/types,types,AvatarService}.ts, packages/avatar/README.md, config.example.jsonc |
| 2026-04-21 (2) | per-target leadMs/lagMs, accompaniment, action variants, enqueue jitter | packages/avatar/src/compiler/{types,action-map,AnimationCompiler}.ts, packages/avatar/src/AvatarService.ts, packages/avatar/assets/default-action-map.json, packages/avatar/README.md, config.example.jsonc |
| 2026-04-21 (3) | tools/vrma-to-clip/ 离线转换工具 + 合成 fixture + 四项单测场景 + README | tools/vrma-to-clip/{package.json,tsconfig.json,index.ts,index.test.ts,README.md,src/*.ts,fixtures/build-synthetic.ts,fixtures/synthetic.vrma.glb} |
| 2026-04-21 (4) | WalkingLayer MVP：`CompilerConfig.walk` + 默认栈含 WalkingLayer + `walkTo`/`stopWalk`/`getCurrentPosition` | packages/avatar/src/compiler/{types.ts,layers/default-layers.ts}, packages/avatar/src/AvatarService.ts, AvatarService.walking.test.ts |
| 2026-04-21 (5) | IdleClip `kind:'quat'` 轨道：IdleClipTrack 判别联合、vrma-to-clip 启发式、sampleClip slerp、AnimationCompiler quat 通道合约、VRMA_01..07 重转 + clip-schema.md | packages/avatar/src/compiler/layers/clips/types.ts, packages/avatar/src/compiler/clips/sampleClip.ts, packages/avatar/src/compiler/AnimationCompiler.ts, tools/vrma-to-clip/src/{validateSchema,sampleTrack,convert}.ts, packages/avatar/docs/clip-schema.md, assets/clips/vrm/VRMA_0*.json |
| 2026-04-22 | Avatar 删除 restPose 机制 + `IdleMotionLayer` freeze-on-gate-exit + resume-from-frozen-frame；loop clip 成为 idle 姿态唯一来源 | packages/avatar/src/compiler/rest-pose.ts (deleted), packages/avatar/src/compiler/{AnimationCompiler,types}.ts, packages/avatar/src/compiler/layers/IdleMotionLayer.ts, IdleMotionLayer.test.ts, packages/avatar/{README.md,src/AvatarService.ts,src/types.ts}, config.example.jsonc |
| 2026-04-22 (2) | Avatar layer 路径补齐 quat 输出：`AnimationLayer.sampleQuat?()`、`LayerManager.sample` → `LayerFrame {scalar, quat}`、IdleMotionLayer cache + `advanceAndSample` 拆分、AnimationCompiler.tick 第 4b 步消费 quat（解决 peace_sign 肘部 quat 被丢弃导致的"假 A-pose"） | packages/avatar/src/compiler/layers/{types,LayerManager,IdleMotionLayer,IdleMotionLayer.test}.ts, packages/avatar/src/compiler/AnimationCompiler.ts |
| 2026-04-22 (3) | Avatar 三项优化：sampleClip scalar 二分搜索（O(n)→O(log n)，~20× on peace_sign）、root-channel contract（sampleClip 跳过 `vrm.root.*`，Walking 独占）、`baselineHalfLifeMs` 45000→3000（loop clip 已拥有 resting pose，短半衰期让 envelope endPose 尽快交回 channel） | packages/avatar/src/compiler/clips/sampleClip.ts, AnimationCompiler.ts, AnimationCompiler.test.ts, AnimationCompiler.tunable.test.ts, types.ts, packages/avatar/src/types.ts, README.md, config.example.jsonc |
| 2026-04-22 (4) | Avatar model-aware handshake Task 3: README docs (hello contract, currentModelKind, layer/action modelSupport, /action-map filtering, restPose deviation), full acceptance (typecheck/lint/smoke-test/bun test 82/82), commit + push | packages/avatar/README.md |
| 2026-04-22 (5) | [Walk 2/2] Walk-cycle polish Task 3: README walk-cycle section (cycleClipActionName, bone-only overlay, rate-factor, slide-mode degradation), acceptance (typecheck/lint/smoke-test pass, 262 pass 2 pre-existing fail), walk_forward asset absent → action-map skip documented | packages/avatar/README.md |
| 2026-04-22 (6) | WalkingLayer 两 bug 修复 + absolute-scalar bypass 第三路径：`AnimationLayer.scalarIsAbsolute`/`LayerFrame.scalarBypass`、`quatFrameChannels`→`bypassFrameChannels`、WalkingLayer `sampleQuat()` split + 构造函数 `??` 合并防 NaN | packages/avatar/src/compiler/{AnimationCompiler,layers/{types,LayerManager,WalkingLayer,IdleMotionLayer}}.ts |
| 2026-04-22 (7) | Channel-based action modelSupport 自动推导（Path A+B）：`vrm.*` 前缀 → vrm-only；8 个 head/body/eye-only action 去掉显式 `modelSupport:'cubism'`；arm-heavy 6 个保留（idle clip 冲突）；TODO-C 在 learnings | packages/avatar/src/compiler/{action-map,AnimationCompiler.test,action-map.test}.ts, packages/avatar/assets/default-action-map.json |
| 2026-04-22 (8) | HUD walk 语义化重构：WalkingLayer 重写为 `Motion` union（linear + orbit），semantic 原语 `walkForward`/`strafe`/`turn`/`orbit` + angularSpeed + `walkTo` 下层保留；WS 协议合并为 `walk-command` discriminated union；PreviewStatus.rootPosition 广播；HUD WalkPanel 全重写 semantic + tunable slider | packages/avatar/src/{compiler/layers/WalkingLayer,AvatarService,preview/{types,PreviewServer}}.ts + renderer commits |
| 2026-04-22 (SN) | Avatar Renderer Capabilities API：renderer → bot WS capability report（`CapabilitiesMessage` + `RendererCapabilities`）；VRM 1.0 18-name `CANONICAL_EXPRESSIONS` 规范词汇；per-connection Map 存储能力快照（WebSocket key，close-hook 清理）；`GET /capabilities` HTTP introspection；`AvatarService.listConnectedCapabilities()`；`TODO(capability-gating)` 占位 | packages/avatar/src/preview/{types,PreviewServer}.ts, packages/avatar/src/AvatarService.ts, +2 test files |
| 2026-04-22 (9) | Rich-tag prompt 改写（legacy `[LIVE2D:]` 从 prompt 撤离，parser shim 保留）+ `outputFps` 30→60（144Hz 屏上视觉阶跃问题根因是发送率/显示率错配，不是 blending）+ bot perf log 门控（`AVATAR_PERF_LOG=1` 启用 tick/emit/gap/handler 聚合） + renderer 60fps 渲染 cap（VrmRenderer.loop gate） | packages/avatar/src/compiler/AnimationCompiler.ts, packages/avatar/src/types.ts, prompts/avatar/{partials/tag-spec,speak-system,emotion-system,bilibili-batch-system}.txt, config.example.jsonc, packages/avatar/README.md |
| 2026-04-22 (10) | `[W:...]` walk tag 系统：parser 新增 walk kind + 10 种语法（forward/back/strafe/turn/orbit/to/face/stop）+ AvatarService `walkToSemantic` / `faceSemantic` + STAGE_POSITIONS 约定（camera=+Z, facing=0）。顺手修 LLMStage streaming 把 `parseLive2DTags` 升到 `parseRichTags`，emotion/gaze/hold/walk 所有 kind 通过新的共享 `dispatchParsedTag` helper 分派。prompt tag-spec 加"移动"节 + speak-system `[W:to:camera]` few-shot | packages/avatar/src/tags/{types,tagParser,index}.ts, packages/avatar/src/{index,AvatarService}.ts, packages/bot/src/services/live2d/{dispatchParsedTag (new),stages/{TagAnimationStage,LLMStage}}.ts, prompts/avatar/{partials/tag-spec,speak-system}.txt |
| 2026-04-22 (11) | Live2D + bot：DeepSeek reasoner 空 `content` / TTS 只读 `content`；`LLMStage` 默认 `maxTokens` 2048；`SpeechService` 预取下一句、去掉按上一句墙钟播放时长阻塞；删 `live2dMaxHistoryMessages` + `PromptAssembly` 全量 `getHistoryEntries`；长线程仍走 `ThreadContextCompressionService`；`writeFileUnderDirectory` 与 `ResourceDownloader` 复用；`mergeAvatarConfig` 结构化合入；SoVITS `prompt_text` 与 ref 不一致 → RMS 极低 | 见 `.claude-workbook/2026-04-22.md` §11；`.claude-learnings/avatar.md` 同题小节 |
| 2026-04-24 | Avatar Phase 2 autonomous API + rate multipliers（ticket `2026-04-24-avatar-autonomous-api`）：`StateNodeSource='llm'\|'autonomous'` + `StateNode.source?`；`enqueueAutonomous(name, intensity, opts?)` + `enqueueAutonomousEmotion(name, intensity)` 共享 `_enqueueModulated`/`_applyEmotionBaseline`；`BreathLayer.setRate()` + `AutoBlinkLayer.setRate()` clamp [0.2,3.0] NaN-no-op；459 tests green | packages/avatar/src/{compiler/types,AvatarService,index}.ts, packages/avatar/src/compiler/layers/{BreathLayer,AutoBlinkLayer}.ts, packages/avatar/src/{__tests__/AvatarService.{autonomous,modulation},compiler/layers/__tests__/{Breath,AutoBlink}Layer}.test.ts |
