**当前群名称**: {{groupName}}
**日期**: {{date}}

## 预计算统计数据

以下数字已由系统算好。这一次的任务用到它们时直接读，没有用到就跳过，不要重新统计：

- 总消息数: {{totalMessages}}
- 活跃成员数: {{activeMembers}}
- 最活跃时段: {{highlightTimeRange}}
- 24小时活跃度（JSON）: {{hourlyActivityJson}}

### 成员发言统计（按消息数排序）
{{memberStats}}

## 昨日聊天记录

以下是昨日的聊天记录，不含你自己的发言，过长的单条已截断（格式: [时间] 昵称(userId): 内容）：

{{chatHistory}}
