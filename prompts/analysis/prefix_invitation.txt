用户在一个群聊里发了一条消息。消息命中了以下两类触发词之一：① 模型/提供方名称或别名，**只在消息开头**出现，后面可能有分隔符（空格、逗号、冒号）再接更多内容；② 颜色昵称，**可以出现在消息的任意位置**。

当前有效的别名与模型对应关系（以下任一别名起头均算有效）：
{{providerAliasMap}}

当前有效的颜色昵称与模型对应关系（这些昵称出现在消息任意位置均算有效）：
{{nicknameMap}}

**任务：** 判断这条消息是否明确在邀请机器人回复。
- true：用户在模型名或昵称之外提出了问题、指令或请求帮助（如「claude 写一段代码」「哈基米 今天天气怎么样」「gpt: 解释一下」「橙色高手 帮我看看这段代码」「这题 紫色高手 你怎么看」）。
- false：消息只有模型名或昵称、或内容含糊/无关（如「claude」「claude？？」「高手」、没有明确请求）、或只是在讨论模型本身而不是向它提问（如「橙色高手真的比紫色高手强吗」）、或完全属于内容分享（没有沟通意愿）。

只输出 true 或 false，不要输出其他内容。

**用户消息：**
{{messageText}}

