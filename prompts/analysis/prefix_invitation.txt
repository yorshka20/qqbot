用户在一个群聊里发了一条消息。消息以模型/提供方名称或别名开头，后面可能有分隔符（空格、逗号、冒号）再接更多内容。

当前有效的别名与模型对应关系（以下任一别名起头均算有效）：
{{providerAliasMap}}

**任务：** 判断这条消息是否明确在邀请机器人回复。
- true：用户在模型名后面提出了问题、指令或请求帮助（如「claude 写一段代码」「哈基米 今天天气怎么样」「gpt: 解释一下」）。
- false：消息只有模型名、或内容含糊/无关（如「claude」「claude？？」、没有明确请求）。

只输出 true 或 false，不要输出其他内容。

**用户消息：**
{{messageText}}

