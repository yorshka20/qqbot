请将以下回复内容重新组织为卡片格式 JSON。

**重要：必须返回有效的 JSON 格式，不要包含任何其他文字说明。**

根据内容类型选择合适的卡片格式：

**1. 基础问答 (type: "qa")**
{
  "type": "qa",
  "question": "用户的问题",
  "answer": "你的回答（可以包含 **加粗** 和 *强调*）"
}

**2. 列表推荐/步骤 (type: "list")**
{
  "type": "list",
  "title": "标题",
  "emoji": "📋",
  "items": ["第一项", "第二项", "第三项"]
}

**3. 重要提示 (type: "info")**
{
  "type": "info",
  "title": "提示标题",
  "content": "详细内容",
  "level": "info"  // 可选值: "info", "warning", "success", "tip"
}

**4. 对比分析 (type: "comparison")**
{
  "type": "comparison",
  "title": "对比主题",
  "items": [
    {"label": "价格", "left": "选项A的价格", "right": "选项B的价格"},
    {"label": "性能", "left": "选项A的性能", "right": "选项B的性能"}
  ]
}

**5. 知识解释 (type: "knowledge")**
{
  "type": "knowledge",
  "term": "专业术语",
  "definition": "定义解释",
  "examples": ["例子1", "例子2"]  // 可选
}

**6. 数据统计 (type: "stats")**
{
  "type": "stats",
  "title": "统计标题",
  "data": [
    {"label": "指标1", "value": "100", "highlight": true},
    {"label": "指标2", "value": "50"}
  ]
}

**格式要求：**
- 必须返回纯 JSON，不要包含 markdown 代码块标记（如 ```json）
- 必须使用双引号，不要使用单引号
- 选择最适合内容类型的卡片格式
- 保持内容简洁、清晰、友好

**内容中的 HTML（仅用于 answer、content、definition、items 项、对比项 left/right 等字符串字段内部）：**
请直接写 HTML（不要写 markdown），以利于友好排版和最终结论的可读性。允许的标签及用法、样式说明如下：

| 标签 | 用法 | 渲染效果 |
|------|------|----------|
| p | 包裹段落；多段结论时每段用 p 分开 | 段间距适中、行高舒适，适合作为主要叙述 |
| strong | 包裹需要加粗的关键词、结论、术语 | 深色加粗，适合强调重点 |
| em | 包裹需要轻微强调的补充说明 | 紫色底、圆角，与 strong 区分 |
| code | 行内代码：命令名、变量、短代码片段 | 等宽字体、浅灰底、圆角，与正文区分 |
| pre + code | 多行代码块：完整命令、配置、示例代码 | 独立块、等宽、浅灰底、保留换行，适合长代码 |
| h2 | 内容内的小节标题（比卡片主标题小一级） | 略大加粗、上下留白，用于分段 |
| h3 | 内容内的子标题 | 再小一级，层次清晰 |
| ul + li | 无序列表：要点、选项、无顺序的条目 | 每项前圆点、缩进、项间距适中 |
| ol + li | 有序列表：步骤、排名、有顺序的条目 | 每项前数字、缩进 |
| table + thead + tbody + tr + th + td | 表格：对比项、多列数据、规格说明 | 边框、表头深色、单元格对齐；可加 class="content-table" 使用统一样式 |
| br | 同一段落内强制换行 | 仅当确实需要行内换行时使用，优先用 p 分段 |

**排版建议（引导友好输出）：**
- 结论先行：先用一两句 p 概括结论或直接回答，再展开细节。
- 分块清晰：长回答用 h2/h3 分小节，用 ul/ol 列要点或步骤，用 table 做对比或参数。
- 重点突出：关键结论、术语用 strong，补充说明用 em；命令、代码用 code 或 pre+code。
- 禁止：仅允许以上标签；禁止 script、style、iframe 及未列出的标签。

**示例 answer（结论 + 要点 + 代码）：**
<p>可以用以下命令查看版本。</p><h3>步骤</h3><ol><li>打开终端</li><li>执行：<code>node --version</code></li></ol><p>多行示例：</p><pre><code>npm install
npm run build</code></pre>

需要转换的回复内容：
{{responseText}}
