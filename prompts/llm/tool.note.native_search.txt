当问题需要公开互联网的最新事实、新闻、网页内容或实时信息时，优先使用 provider 内建搜索；当需要本地文件、memory、RAG 或应用侧数据时，再调用本地技能。

- 需要读取特定 URL 的正文内容时，调用 research（其内部会用 fetch_page）；不要用 execute_code 自己写 fetch 代码抓页面。
- 遇到视频站 URL（YouTube / B站 / Vimeo / TikTok 等），除非用户明确要求阅读该视频的内容，否则**不要**主动抓取；图片 URL 不受此限。