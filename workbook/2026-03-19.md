# Claude Code 工作日志 - 2026-03-19

## 知乎 Feed 订阅服务实现

### 任务描述

根据 `src/services/zhihu/zhihu-feed-implementation-plan.md` 实现知乎关注动态的拉取、存储和摘要推送服务。

### 实现内容

#### 新增文件

| 文件 | 说明 |
|------|------|
| `src/services/zhihu/types.ts` | 知乎 API 响应类型、统一内容结构、数据库行类型、配置接口 |
| `src/services/zhihu/tokens.ts` | DI tokens（零导入，避免循环依赖） |
| `src/services/zhihu/ZhihuClient.ts` | HTTP 客户端，封装 cookie 管理、限速、重试、cookie 有效性检查 |
| `src/services/zhihu/ZhihuContentParser.ts` | Feed item 解析器，将异构 verb/target 类型统一为 `ZhihuContentItem` |
| `src/services/zhihu/ZhihuDatabase.ts` | 独立 SQLite 数据库（`data/zhihu.db`），类似 WeChat 的隔离模式 |
| `src/services/zhihu/ZhihuFeedService.ts` | Feed 拉取 + 去重 + 存储主服务 |
| `src/services/zhihu/ZhihuDigestService.ts` | 摘要生成（LLM）+ QQ 群推送 |
| `src/services/zhihu/index.ts` | Barrel export |
| `src/plugins/plugins/ZhihuFeedPlugin/index.ts` | Plugin wrapper，DI 注册 + node-cron 调度 |

#### 修改文件

| 文件 | 说明 |
|------|------|
| `config.example.jsonc` | 新增 zhihuFeed 插件配置段 |

### 技术决策

1. **独立数据库模式**: 参照 WeChat 服务，使用独立 `data/zhihu.db`，不依赖核心 DatabaseManager
2. **Plugin 模式**: 注册为 `zhihuFeed` 插件，配置在 `plugins.list` 中，符合项目 plugin 架构
3. **直接 node-cron**: 使用 `node-cron` 而非 AgendaService，因为 poll/digest 是确定性操作，不需要 LLM 驱动的 AgentLoop
4. **Verb 过滤**: 实际 API 返回的 verb 与文档描述有差异（如 `MEMBER_VOTEUP_ANSWER` vs `ANSWER_VOTE_UP`），已更新默认 filter

### 遇到的问题

1. **`stripHtml` 参数类型**: 知乎 API 的 `excerpt`/`content` 有时不是 string（可能是 object），导致 `.replace()` 失败。修复：将 `stripHtml` 入参改为 `unknown` 类型并增加类型检查。
2. **`as const` 与 mutable 类型不兼容**: `DEFAULT_ZHIHU_CONFIG` 使用 `as const` 后，其 `verbFilter` 变为 readonly，与 `ZhihuConfig.verbFilter: string[]` 不兼容。修复：在 plugin 中手动展开默认值而非使用 intersection type。

### 验证结果

- typecheck: ✓
- lint: ✓（新文件无 error/warning）
- smoke-test: ✓（所有服务正常初始化）
- 实际 API 测试: ✓（cookie 有效，成功拉取 21 条动态，解析并存储 18 条）
