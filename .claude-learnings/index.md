# Learnings Index

| Scope | 文件 | 内容摘要 |
|-------|------|---------|
| mind | [mind.md](mind.md) | **NEW** 双系统心智模型设计（`packages/bot/src/mind/`，Phase 1 未实施）；真系统 1 = 代码（ODE / baseline / modulation table），快聊天 LLM（Groq/DeepSeek）≠ 系统 1，系统 2 = 反思（Gemini/Claude，分钟级）；DGM 三层（Core DNA JSON / Epigenetics SQLite / Phenotype 内存 + snapshot）；relationships 用 JSON 列不规范化 + `extra_json` escape hatch；trait 24h 漂移 bound 0.1，forbiddenWords 只追加；Phenotype ODE 时间常数（τ_valence=10min/τ_arousal=3min/τ_dominance=30min/τ_attention=2min）；Strategy ≠ Mood（离散指令 vs 连续状态）；**Tone Vocabulary 强 enum**（neutral/playful/sarcastic/affectionate/tsundere/melancholy/excited/weary/professional/dismissive/coy/defiant），三端同步（prompt 片段 + modulation delta + variant weights）；**PersonaModulation 六组按 pipeline 注入点分组**（Amplitude/Timing/Spatial/Emotion/ActionPref/Ambient），不按人格特质；Prompt Patch 通路（System 2 写 state，Assembler 确定性翻译，不让 LLM 写 prompt 给另一个 LLM）；Memory vs Epigenetics 分库互引不合并（`Relationship.sharedMemoryRefs` 存 Memory ID）；**Avatar compiler 改造**（Phase 1: `enqueueTagAnimation` modulation 注入点 + `CHANNEL_GROUPS` 显式化 + `resolveAction` variant weights；Phase 2: `enqueueAutonomous{,Emotion}` + `BreathLayer/BlinkLayer.setRate`；Phase 3: `PersonaPostureLayer` 加性 bias + Gaze 分布 + ambientGain 多源 min）；和 agenda 共享 `InternalEventBus` 不互依赖；完整设计见 `docs/local/mind-system-design.md` |
| bilibili | [bilibili.md](bilibili.md) | VideoKnowledgeClient DI 注入 + BilibiliService/VideoKnowledgeClient 解耦 + `/b站` 子命令分发；+ **直播弹幕 WS 接入**：16 字节头协议、`-352` 三层根因（裸 getDanmuInfo → WBI 必签、buvid3 必带、`platform='danmuji'` 绕浏览器指纹、SESSDATA+danmuji 冲突必匿名）、`code=1006 Connection ended` 是服务器 RST 不是网络、`reconnectAttempts` 归零必须等 AUTH_REPLY code=0 不能在 onOpen、SESSDATA 从 DevTools 拿是解码形态（自动 URL-encode 归一）、架构链 `LiveClient → DanmakuBuffer → DanmakuStore → LiveBridge → Live2DPipeline` |
| video-tools | [video-tools.md](video-tools.md) | Gemini 视频 API、VideoDownloadService/ResourceCleanupService、SubAgent 返回 string、extractVideoUrl 裸域名/BV/shorts、light_app 视频 URL 提取（segmentsToText + handler 解析） |
| cluster | [cluster.md](cluster.md) | hub_spawn child 共享 jobId/taskCount=1（只 root 推进计数器）、WorkerRegistry 纯内存重启即丢、/workers 端点 union live + cluster_tasks 历史重建、worker 在 backend.spawn 之前就注册；+ batch10: `tickets/<id>/results/job.json` 跨 LAN snapshot（clusterId + workers + tasks），re-dispatch 覆盖语义 + stale task-*.md 清理 + hostname fallback |
| webui | [webui.md](webui.md) | Tailwind v4 + 少量 Radix primitives、Radix Select 空值禁用/Portal 字色不继承、ticket editor paste-lift frontmatter、Project/Template select 未知值语义、三个 Select 组件复用分布 |
| avatar | [avatar.md](avatar.md) | **2026-04-24 新增**: `StateNodeSource='llm'\|'autonomous'` + `StateNode.source?`；`enqueueAutonomous(name, intensity, opts?)` + `enqueueAutonomousEmotion(name, intensity)` 共享 `_enqueueModulated`/`_applyEmotionBaseline` helper；`BreathLayer.setRate()` + `AutoBlinkLayer.setRate()` clamp [0.2,3.0] NaN-no-op identity=1.0；already-scheduled `nextBlinkAt` 不回溯修正。**2026-04-23 新增 (2)**: PerlinNoiseLayer activity envelope（two-perlin 架构，motion × slow envelope，`envPauseBelow`/`envActiveAbove` 两阈值 smoothstep，per-channel 独立 envelope seed，`envActiveAbove:-10` escape hatch，不用状态机的原因，不改 `setWeight(0.2)` 的理由）。**2026-04-23 新增**: SpeechService 双路径（drainOneBuffered / drainOneStreaming）；AudioChunkMessage schema；零延迟合约；AudioEnvelopeLayer streaming mode 为何返回 {}；PCM-only 流式 RMS 原因；fallback 策略。+ 设计基座：AI 写剧本（语义标签）+ 本地编译器导演（**默认 60Hz 参数流** + renderer 侧 60fps 渲染 cap）；`@qqbot/avatar` 包不 import bot 代码，`packages/bot/src/services/live2d/*` 5-stage pipeline；`AvatarService` 编排 compiler / layer 栈 / `WalkingLayer`（MVP API）/ `AmbientAudioLayer` / VTS / PreviewServer(0.0.0.0:8002, `/action-map` `/clip/:name` `/capabilities` + WS trigger/speak/ambient-audio/tunable-*/capabilities) / SpeechService；AnimationCompiler 双路径（envelope ADSR + clip 采样）+ channelBaseline / crossfade / endPose 三件套 + per-target leadMs/lagMs / accompaniment / variants / duration+intensity jitter；IdleClip v2 `kind:'quat'` 大角度骨骼（>π/2）四元数轨道 + compiler slerp-with-identity；6 默认 layer（Breath/AutoBlink/EyeGaze/IdleMotion/Perlin/AmbientAudio），AudioEnvelopeLayer per-utterance ephemeral；Activity = {pose, ambientGain} 两轴正交；**Rich tag `[A:] [E:] [G:] [H:] [W:]`（legacy `[LIVE2D:]` parser shim 保留但不再在 prompt 里 teach）**；Cubism vs VRM 两套 channel registry；`{{availableActions}}` 动态注入 prompt；TTSManager 多 provider + 共享 audio-envelope-config 支持 mid-utterance tunable；VTS 只接 tracking param 靠 VTS_CHANNEL_MAP 翻译；`tools/vrma-to-clip/` 离线转换；**restPose 机制已删除（loop clip 是 idle 姿态唯一来源）+ IdleMotionLayer freeze-on-gate-exit + Layer 路径 quat 输出**；**Model-aware handshake：`hello { modelKind, protocolVersion:1 }` → `currentModelKind` last-hello-wins；`AnimationLayer.modelSupport` + `ActionMapEntry.modelSupport 'cubism'\|'vrm'\|'both'` 缺省=both**；**WalkingLayer walk-cycle clip**；**Absolute-scalar bypass（`AnimationLayer.scalarIsAbsolute` + `LayerFrame.scalarBypass`）**；**Action modelSupport 自动推导（vrm.\* 前缀 → vrm-only）**；**WalkingLayer semantic motion system：`Motion = LinearMotion \| OrbitMotion`；语义 API `walkForward/strafe/turn/orbit`；WS 协议 `walk-command` discriminated union**；**Renderer Capabilities API：`CANONICAL_EXPRESSIONS`（VRM 1.0 18 presets）规范词汇**；**`[W:...]` LLM 位移 tag（forward/back/strafe/turn/orbit/to/face/stop）+ 舞台约定 `camera=+Z,facing=0` 让 LLM 不需要知道真实相机位置；LLM 度数 → 代码转弧度；cubism 自动 no-op**；**`LLMStage` streaming flusher 升级到 `parseRichTags` 全 kind 分派（修了 emotion/gaze/hold/walk 在 streaming 路径被 drop 的老 bug，共享 `dispatchParsedTag` helper）**；**outputFps 默认 60（原 30），对 144Hz 屏幕视觉阶跃问题的直接修复；AVATAR_PERF_LOG=1 环境变量 gate bot perf log（renderer 侧不留 log 仪表防浏览器崩页）**；**2026-04-22 [11]：LLMStage `maxTokens` 2048、DeepSeek 空 `content`+reasoning 警告、SpeechService prefetch、删 `live2dMaxHistoryMessages` 与全量 `getHistoryEntries`、长线程 `ThreadContextCompressionService`、`writeFileUnderDirectory`、`mergeAvatarConfig`、SoVITS prompt/ref 一致（RMS）** |
| monorepo | [monorepo.md](monorepo.md) | bun workspaces、packages/bot 目录布局与依赖归属、运行时资产 cwd 约定、project references 配置、@/ alias 新映射、PluginManager hardcoded path 修复、allowImportingTsExtensions 移除原因；T3 avatar 抽包要点（hoisting、pre-commit hook 限制、inline import 陷阱、repoRoot 复制 vs logger shim） |
